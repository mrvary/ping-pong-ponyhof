{
  "name": "electronTest",
  "version": "1.0.0",
  "main": "src/main.js",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "start-dev": "concurrently \"cross-env NODE_ENV=dev nodemon --exec \"\"electron .\"\"\" \"cd ./src/client/ && yarn start\"",
    "postinstall": "electron-rebuild",
    "electron-rebuild": "electron-rebuild"
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "cross-env": "^6.0.3",
    "electron": "^7.1.0",
    "electron-rebuild": "^1.8.6",
    "nodemon": "^2.0.1"
  },
  "dependencies": {
    "electron-log": "^3.0.9",
    "express": "^4.17.1",
    "node-expat": "^2.3.18",
    "socket.io": "^2.3.0",
    "xml2json": "^0.11.2"
  }
}
